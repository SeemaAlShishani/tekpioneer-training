# Tasks CLI
A simple Node.js command-line task manager.

Tasks are saved in `tasks.json`, so they persist after the program ends.

## Commands

- Add: `node tasks.js add "study JSON"`
- List: `node tasks.js list`
- Done: `node tasks.js done 1`
- Delete: `node tasks.js delete 1`
